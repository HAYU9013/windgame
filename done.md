# 開發紀錄

## 2025-11-15
- 完成環境初始設置：建立 `requirements.txt` 指定 pygame 版本需求。
- 建立專案目錄結構：新增 `src/`、`assets/`、`data/`、`docs/` 等資料夾並配置 `.gitignore`。
- 建立基礎資料檔：撰寫縣市、水庫與族群偏好 JSON，作為系統計算依據。
- 完成資料載入模組與資料結構，提供縣市、水庫與族群資訊查詢。
- 建立 `GameState` 資源運算，串接神力消耗、滿意度與新聞隊列。
- 實作地圖、控制面板、跑馬燈與滑桿按鈕等 pygame 介面元件。
- 建立主程式迴圈 `src/main.py`，設定 1280x720 視窗與時間流速控制。

## 2025-11-16
- 新增 `assets.md`，統整各類素材與預計檔案名稱，方便資產製作與匯入規畫。
- 建立 `assets/` 與 `docs/` 目錄並加入 `.gitkeep`，完成資產與文件目錄初始化。
- 新增 `data/events.json` 並擴充資料載入層，支援季節與特殊事件定義。
- 建立 `EventManager` 模組，整合事件修正值至資源、災損與神力成本計算流程。
- 優化 `GameState`，加入月份、季節判定與事件影響後的降雨、風勢、滿意度邏輯。
- 更新控制面板顯示，新增時間資訊與事件摘要，同步調整資源區塊排版。
- 改寫新聞跑馬燈快取機制，確保新訊息加入時即時重播滾動內容。

## 2025-11-17
- 撰寫單元測試涵蓋資料載入、事件管理與遊戲狀態運算，確保核心模組行為穩定。
- 建立自動化測試案例驗證神力消耗、資源回饋、事件新聞觸發與格式化工具函式。
- 透過 pytest 執行測試，確認迴圈更新、水庫蓄水計算與滿意度評估皆符合預期。

## 2025-11-18
- 擴充 `GameState`，加入「島嶼脈動」焦點摘要，整合水情、民意與季節態勢，讓狀態回饋更具臨場感。
- 更新控制面板介面，新增焦點區塊並調整事件列排版，提升資訊層次與遊戲敘事張力。
- 擴寫單元測試覆蓋焦點摘要邏輯，驗證不同條件下的提示內容與季節敘述。

## 2025-11-19
- 建立地圖素材讀取流程，若實際檔案缺席即以方框與多邊形描繪臺灣輪廓，確保原型不會因資產缺漏而崩潰。
- 新增字型載入防呆機制，遇到路徑錯誤自動改用系統預設字型維持介面顯示。
